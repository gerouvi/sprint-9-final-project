import logoMemoWords from '../../assets/images/memoWordsTr.png';
import { Background, LogoMemoWords } from './LockMobile.styles';

const LockMobile = () => (
  <Background>
    <LogoMemoWords logoMemoWords={logoMemoWords} />
  </Background>
);

export default LockMobile;

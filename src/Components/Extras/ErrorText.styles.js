import styled from 'styled-components';
import { THEME_STYLES } from '../../styles/THEME_STYLES';
import ErrorText from './ErrorText';

export const ErrorTextStyled = styled(ErrorText)`
  color: ${THEME_STYLES.COLORS.RED};
`;
